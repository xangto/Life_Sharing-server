server:
    servlet:
        context-path: /api
spring:
    application:
        name: life
    servlet:
        multipart:
            max-file-size: 10MB
            max-request-size: 1000MB
    datasource:
        druid:
            username: root
            password: xangto123
            driver-class-name: com.mysql.cj.jdbc.Driver
            url: jdbc:mysql://localhost:3306/life-server?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true

jwt:
    issure: jQiaos
    screct: xangto-jianqiao-best
    expires_time_hours: 10
mybatis-plus:
    configuration:
        map-underscore-to-camel-case: true
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    global-config:
        db-config:
            id-type: assign_id

springdoc:
    api-docs:
        enabled: true
    swagger-ui:
        path: /swagger-ui.html
    override-with-generic-response: false
knife4j:
    enable: true
    setting:
        language: zh_cn